---
title: "安装插件"
tags: ["Claude-Code", "插件"]
updated: 2026-01-11
---
## 关于插件

Claude Code 安装后自带了一些技能（Skills），不需要额外装插件就能用。

所谓的"插件"其实是指这些内置技能，不是真的要去装什么东西。

## 查看有哪些技能

```bash
claude code
/plugin
```

![[iShot_2026-01-11_21.28.00.mp4]]


## 执行官方初始化命令

```
/init
```

该命令会自动分析项目结构，生成初始的 `CLAUDE.md` 项目文档。

---

## 功能开发

使用 Feature Dev 插件进行功能开发：

```
/feature-dev:feature-dev
```

**使用技巧：**

- 尽量不要直接回车发送，可能会丢失信息
- 建议先完整描述需求后再发送

---
## 代码审查

功能开发完成后，使用以下命令执行代码审查：

```
/code-review
```

该命令会：

- 自动分析代码变更
- 执行代码审查

---

## 代码提交

功能开发完成后，使用以下命令生成规范的 Git Commit：

```
/commit-commands:commit
```

该命令会：

- 自动分析代码变更
- 生成符合规范的 commit message
- 支持多种 commit 风格

---

## 文档维护

当项目有重大变更时，可以再使用`/init` 命令更新 `CLAUDE.md`：

---

[[前置准备]] | [[常用斜杠命令]]
